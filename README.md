# Pasos para el flujo basico de git
```sh
git status
git add . o git add -a
git status 
git commit -m "Descripcion del cambio"
git push 
```

## Notas
El mensaje del commit debe de ser una oracion descriptiva del cambio
